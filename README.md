# X-ray Server Setup — Автоматическая установка VPN-сервера

Автоматизированная установка X-ray сервера с **VLESS + Reality + Vision** на Ubuntu 22/24.

## 📋 Что нужно:

- VPS с Ubuntu 22 или 24
- 5-10 минут времени
- Минимальные требования: 1 CPU, 1 GB RAM, 10 GB диска

---

## 🚀 Установка (3 команды)

<details>
<summary><strong>🐣 Я новичок. Расскажите подробно, как подключиться и вводить пароли? (Нажмите, чтобы развернуть)</strong></summary>

### 1. Чем подключаться к серверу?

Вам пришло письмо от хостинга с IP-адресом и паролем `root`.
Чтобы зайти на сервер, удобнее всего использовать специальные программы:

- **Windows:** [MobaXterm](https://mobaxterm.mobatek.net/) (рекомендую!) или [Termius](https://termius.com/).
- **macOS:** Стандартный Терминал или [Termius](https://termius.com/).
- **Android/iOS:** [Termius](https://termius.com/).

В этих программах вы просто создаете новый хост, вводите IP, логин `root` и пароль из письма.

### 2. Почему не печатается пароль?

В Linux есть особенность безопасности: **когда вы вводите пароль в консоли, символы (даже звездочки) НЕ отображаются.**
Вам может показаться, что клавиатура не работает. Это не так!
👉 **Просто введите пароль "вслепую" и нажмите Enter.**

### 3. Что отвечать на вопросы про "Room Number"?

Когда скрипт будет создавать нового пользователя, он спросит:

- `Full Name []`:
- `Room Number []`:
- `Work Phone []`:
  И т.д.

👉 **Это необязательная информация. Просто нажимайте Enter на каждом вопросе, оставляя поля пустыми.**
В конце напишите `Y` (Yes) и нажмите Enter для подтверждения.

### 4. Читайте консоль!

Скрипт пишет всё, что он делает. Если он остановился и ничего не происходит — скорее всего, он ждет вашего ввода (пароля или подтверждения).

</details>

### Шаг 1: Первичная настройка сервера

Подключись к VPS по SSH.

### Если wget не установлен:

apt update && apt install -y wget

Затем запускай скрипт.

Запусти скрипт настройки:

wget -qO- https://raw.githubusercontent.com/Umalanif/xray-server-setup/main/setup.sh | bash

**Что произойдёт:**

- Обновление системы
- Настройка firewall (порты: 22, 80, 443)
- Создание нового пользователя с sudo-правами
- Отключение root-входа по паролю

**⚠️ ВАЖНО:** Не закрывай терминал! Сначала протестируй нового пользователя:

su - твоё*имя*пользователя
sudo whoami # Должно вывести "root"

---

### Шаг 2: Смена DNS на Cloudflare (рекомендуется)

wget -qO- https://raw.githubusercontent.com/Umalanif/xray-server-setup/main/change-dns.sh | sudo bash

**Зачем:** Cloudflare DNS быстрее и не блокирует Tor/некоторые сайты.

---

### Шаг 3: Установка X-ray

wget -qO- https://raw.githubusercontent.com/ServerTechnologies/simple-xray-core/refs/heads/main/xray-install | sudo bash

**Что произойдёт:**

- Установка X-ray ядра с VLESS + Reality + Vision
- Автоматическая настройка
- Вывод QR-кода для подключения

---

## 📱 Подключение к VPN

Получи ссылку для подключения:

Скопируй ссылку (`vless://...`) или отсканируй QR-код в приложении:

### Клиенты для подключения:

- **Windows:** [Nekoray](https://github.com/MatsuriDayo/nekoray), v2rayN
- **Android:** [v2rayNG](https://github.com/2dust/v2rayNG)
- **iOS:** Streisand, Shadowrocket
- **macOS:** V2rayU, Nekoray
- **Linux:** Nekoray, v2rayA

---

## 🔧 Управление пользователями

После установки X-ray у тебя будут доступны удобные команды для управления:

| Команда   | Описание                                           |
| --------- | -------------------------------------------------- |
| userlist  | Вывести список всех клиентов                       |
| mainuser  | Показать ссылку и QR-код основного пользователя    |
| newuser   | Создать нового пользователя                        |
| rmuser    | Удалить пользователя                               |
| sharelink | Создать ссылку для подключения любого пользователя |

### 📖 Справка

В домашней папке пользователя автоматически создаётся файл `help` с подсказками.
Посмотреть его можно с помощью команды (нужно находиться в домашней папке пользователя):

cat help

---

## 📊 Что используется:

- **Протокол:** VLESS с Reality + Vision + uTLS (firefox)
- **Маскировка:** Трафик выглядит как HTTPS к github.com
- **TCP Congestion Control:** BBR (автоматически)
- **DNS:** Cloudflare 1.1.1.1 (если выбран шаг 2)

---

## ❓ Решение проблем

### Проверить статус X-ray:

sudo systemctl status xray

### Перезапустить X-ray:

sudo systemctl restart xray

### Посмотреть логи:

sudo journalctl -u xray -f

---

## 🚑 Проблемы с подключением? (Смена маскировки)

Если ваш провайдер блокирует соединение (или включил "белые списки"), попробуйте сменить маскировку трафика (SNI) на популярные российские сервисы.

**Запустите эту команду на сервере:**
sudo bash <(curl -Ls https://raw.githubusercontent.com/Umalanif/xray-server-setup/main/extras/change_sni.sh
)

Скрипт предложит выбрать домен (VK, Ozon, Wildberries и др.) и автоматически перенастроить сервер.

## 🔀 Плохо работает на мобильном? (Добавить gRPC)

Если ваш оператор блокирует основной протокол (TCP/Vision), добавьте резервный канал через gRPC. Это помогает обойти жесткие фильтры мобильных сетей.

**Выполните эту команду на сервере:**
sudo bash <(curl -Ls https://raw.githubusercontent.com/Umalanif/xray-server-setup/main/extras/add_inbound.sh
)
Скрипт автоматически:

1. Откроет новый порт в Firewall.
2. Настроит gRPC на тех же ключах.
3. Выдаст готовую ссылку `vless://`, которую нужно добавить в приложение как второй сервер.

---

## ❓ Частые вопросы (FAQ)

### 1. Почему не работает на мобильном интернете (МТС, Мегафон и др.)?

Мобильные операторы часто блокируют основной протокол (TCP).
👉 **Решение:** Используйте скрипт "Добавить gRPC" (см. инструкцию выше) и подключитесь через новую ссылку.

### 2. Соединение перестало работать вообще везде (и WiFi, и LTE).

Скорее всего, провайдер заблокировал стандартную маскировку (Microsoft/Google).
👉 **Решение:** Запустите скрипт "Смена маскировки" и выберите российский сервис (VK, Ozon).

### 3. Ошибка "Command not found" или "Permission denied".

Вы забыли `sudo` или не установили `curl`.
👉 **Решение:** Копируйте команды из инструкции целиком, не пропуская `sudo`.

### 4. В приложении ошибка "Time skew" или "Invalid time".

Время на вашем телефоне/ПК отличается от времени на сервере более чем на 90 секунд.
👉 **Решение:** Включите автосинхронизацию времени в настройках вашего устройства.

### 5. Как удалить пользователя?

Зайдите на сервер и введите команду: `rmuser`.

---

## 🙏 Благодарности

- [X-ray Project](https://github.com/XTLS/Xray-core)
- [ServerTechnologies](https://github.com/ServerTechnologies/simple-xray-core)

---
